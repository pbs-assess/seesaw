source("renv/activate.R")

# Parallel install defaults for renv / install.packages()
# ncpus <- suppressWarnings(as.integer(Sys.getenv("SEESAW_NCPUS", unset = "16")))
# if (!is.na(ncpus) && ncpus > 0L) {
#   options(Ncpus = ncpus)
#   if (identical(Sys.getenv("MAKEFLAGS", unset = ""), "")) {
#     Sys.setenv(MAKEFLAGS = paste0("-j", ncpus))
#   }
# }
